name: HeatC
language: c

register: register_bmi_heat
includes:
  - "#include <heat/bmi_heat.h>"
cflags:
  pkgconfig: heatc
libs:
  pkgconfig: heatc

build:
  brew:
    formula: csdms/tools/bmi-c
    options: "--HEAD"
