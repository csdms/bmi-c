language: c
bmi_prefix: BMI_HEAT
bmi_shared_library: libbmi_heatc
build:
  brew:
    formula: csdms/tools/bmi-c
    head: True
include:
  - "#include <heat/bmi_heat.h>"
cflags:
  - pkg-config --cflags heatc
libs:
  - pkg-config --libs heatc
