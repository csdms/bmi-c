name: HeatC
language: c
prefix: BMI_HEAT
build:
  brew:
    formula: csdms/tools/bmi-c
    options: "--HEAD"
includes:
  - "#include <heat/bmi_heat.h>"
cflags:
  pkgconfig: heatc
libs:
  pkgconfig: heatc
grids:
  - uniform_rectilinear
