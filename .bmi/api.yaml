language: c
bmi_prefix: BMI_HEAT
bmi_shared_library: libbmi_heat
build:
  brew:
    formula: csdms/tools/bmi-c
    head: True
include:
  - "#include <bmi_heat.h>"
cflags:
  - pkg-config --cflags heat
libs:
  - pkg-config --libs heat
